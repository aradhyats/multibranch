pipeline{
    agnet any
    stages{
          stage("Build main"){
             steps{
                 script{
                     sh " echo ${BRANCH_NAME} "
                 }
             }
           }
     }
}
